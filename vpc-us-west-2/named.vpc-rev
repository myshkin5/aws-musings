$ORIGIN {{SECOND_OCTET}}.10.in-addr.arpa.
$TTL 86400  ; 1 day
@       SOA {{VPC_NAME}}-dns root (
    0          ; serial
    3600       ; refresh (1 hour)
    3600       ; retry (1 hour)
    604800     ; expire (1 week)
    10800      ; minimum (3 hours)
)
        NS  {{VPC_NAME}}-dns.{{VPC_NAME}}.compute.internal.
; TODO: The third and fourth octet of the DNS server's IP address needs to be updated here ***in reverse order***
1.0     PTR {{VPC_NAME}}-dns.{{VPC_NAME}}.compute.internal.
